import styled from "styled-components";

const Innput = styled.form`
  display: flex;
  width: 47.5%;
  position: relative;
  align-items: center;
`

const Innputt = (props:any) => {
    return <Innput />;
};

export default Innputt;